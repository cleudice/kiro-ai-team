{
  "version": "v1",
  "hooks": [
    {
      "name": "format-dotnet",
      "description": "dotnet format + build incremental ao salvar C#, determinístico (I5 — antes disparava um agente inteiro por save)",
      "trigger": "PostFileSave",
      "matcher": ".*\\.cs$",
      "action": {
        "type": "command",
        "command": "bash .kiro/hooks/scripts/format-dotnet.sh"
      },
      "timeout": 120,
      "enabled": true
    }
  ]
}
