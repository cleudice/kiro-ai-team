{
  "version": "v1",
  "hooks": [
    {
      "name": "format-oracle",
      "description": "Confere guidelines Oracle/PL-SQL ao salvar .sql",
      "trigger": "PostFileSave",
      "matcher": ".*\\.sql$",
      "action": {
        "type": "agent",
        "prompt": "Confira o script SQL/PL-SQL alterado contra steering/guidelines/oracle.md: script de rollback presente, bind variables em vez de concatenação, tratamento de exceção explícito (sem WHEN OTHERS THEN NULL), e se altera objeto compartilhado, dependentes mapeados. Reporte apenas problemas."
      },
      "enabled": true
    }
  ]
}
