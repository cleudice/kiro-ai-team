{
  "version": "v1",
  "hooks": [
    {
      "name": "format-oracle",
      "description": "Linter determinístico contra guidelines/oracle.md ao salvar .sql (M3 — antes disparava um agente inteiro por save)",
      "trigger": "PostFileSave",
      "matcher": ".*\\.sql$",
      "action": {
        "type": "command",
        "command": "bash \"$(bash .kiro/scripts/kiro-paths.sh)/hooks/scripts/check-oracle.sh\""
      },
      "timeout": 30,
      "enabled": true
    }
  ]
}
